import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const organizationsetupImporterSelectors = importerSelectors(
  'organizationsetup.importer',
);

export default organizationsetupImporterSelectors;
